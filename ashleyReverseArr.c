#include <stdio.h>

int reverseArray(int *arr, int size);
int main() {
    int size;
    int i;

    printf("Enter size: ");
    scanf("%d", &size);

    int arr[size];

    printf("Enter the elements: ");
    for (i=0; i<size; i++) {0
        scanf("%d", &arr[i]);
    }

     printf("The elements of the array: ");
 
}
